#usda 1.0
(
    customLayerData = {
        string creator = "URDF USD Converter v0.1.2"
    }
    doc = """Generated from Composed Stage of root layer /tmp/tmp6u9y_wbm/R1.usdc


Generated from Composed Stage of root layer /tmp/urdf_import_R1_r_aparob/usdex_R1/R1.usdc


Generated from Composed Stage of root layer /tmp/urdf_import_R1_r_aparob/temp_R1/R1.usd


Generated from Composed Stage of root layer /home/rriemer/Downloads/r1_description/R1_1/payloads/base.usd
"""
    kilogramsPerUnit = 1
    metersPerUnit = 1
    upAxis = "Z"
)

def Scope "Materials"
{
    def Material "light_grey"
    {
        color3f inputs:diffuseColor = (0.44798842, 0.44798842, 0.44798842)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/light_grey/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/light_grey/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/light_grey.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/light_grey.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/light_grey.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/light_grey.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "white"
    {
        color3f inputs:diffuseColor = (0.8900055, 0.8900055, 0.8900055)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/white/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/white/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/white.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/white.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/white.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/white.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "A"
    {
        color3f inputs:diffuseColor = (1, 0, 0)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/A/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/A/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/A.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/A.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/A.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/A.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "B"
    {
        color3f inputs:diffuseColor = (0, 0, 1)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/B/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/B/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/B.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/B.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/B.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/B.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "dark"
    {
        color3f inputs:diffuseColor = (0.13286833, 0.13286833, 0.13286833)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/dark/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/dark/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/dark.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/dark.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/dark.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/dark.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }
}

